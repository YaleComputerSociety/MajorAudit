
class User:
	def __init__(self, netID, onboard, name, degrees, studentCourses, programs, language):
		self.netID = netID
		self.onboard = onboard
		self.name = name
		self.degrees = degrees
		self.studentCourses = studentCourses
		self.programs = programs
		self.language = language
